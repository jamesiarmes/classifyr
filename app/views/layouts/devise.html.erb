<!DOCTYPE html>
<html>
  <head>
    <title>Datablade</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-800">
    <main class="flex flex-col h-screen justify-center align-middle">
      <div class="flex justify-center h-16">
        <img class="h-12 w-auto" src="<%= image_path 'workflow-logo-indigo-500-mark-white-text.svg' %>" alt="Classifyr">
      </div>
      <div class="w-fit mx-auto px-12 py-8 bg-gray-100 rounded-lg">

        <div class="w-full">
          <% if notice %>
            <p class="notice" id="notice"><%= notice %></p>
          <% end %>
          <% if alert %>
            <p class="alert alert-danger"><%= alert %></p>
          <% end %>
        </div>

        <%= yield %>
      </div>
    </main>
  </body>
</html>
