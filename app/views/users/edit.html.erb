<div class="w-full">
  <%= render 'shared/header',
    title: @user.email,
    description: 'Make updates to an existing user.'
  %>
  <div class="max-w-7xl mx-auto px-4 sm:px-6 md:px-8">
    <div class="my-12 px-4 py-5 sm:px-6">
      <div id="users" class="header">
        <%= render "form", user: @user %>
        <%= link_to 'Cancel', users_path, class: "button-cancel" %>
      </div>
    </div>
  </div>
</div>
